var n1 = 5
var n2 = 2

// + is used to sam
var result = n1 + n2 
`SAM: ${n1} + ${n2} = ${result}`

// - is used to sub
var sub = n1 - n2
`SUBTRATION: ${n1} - ${n2} = ${result}`

// / is used to div
var div = n1 / n2
`DIVISION: ${n1} / ${n2} = ${result}`

// % is used to div rest
var mult = n1 % n2
`DIVISION REST: ${n1} % ${n2} = ${result}`

// * is used to multiplicar(portuguese word)
var mult = n1 * n2
`MULTIPLICATION: ${n1} X ${n2} = ${result}`

// ** is used to potencia(portuguese word)
var mult = n1 ** n2
`POTENCIA: ${n1} ^ ${n2} = ${result}`