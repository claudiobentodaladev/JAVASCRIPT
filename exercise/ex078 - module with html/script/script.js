import { 
    language,
    number_text,
    fullname,
    yearsold
} from "./module.js"

console.log(`My full name ${fullname.name} is ${fullname.surname} ${fullname.lastname}`)
console.log(` And i am ${number_text(yearsold)}`)
console.log(language)