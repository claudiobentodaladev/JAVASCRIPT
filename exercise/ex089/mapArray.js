const names = ['claudio', 'bento', 'dala']
/*
for (const index in names) {
   console.log(names[index])
}
*/

// map is a native function to make the same than forIn.
names.map( index =>{
    console.log(index)
})
